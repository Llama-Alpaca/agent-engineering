# Lab 4: the Lab 1 observer mount, kept under Lab 4's name — run it to produce a
# session whose live event stream (stderr) and durable log can be compared directly.
- id: agent-default-model
  config:
    provider: lab-scripted
    model: lab-echo-1

- insert:
    - id: lab-scripted-llm
      name: '/path/to/your/deepseek-harness/examples/lab-src/scripted-llm.ts'
    - id: lab-hello
      name: '/path/to/your/deepseek-harness/examples/lab-src/hello.ts'
